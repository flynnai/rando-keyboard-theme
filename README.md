# Randomized Keyboard Theme for QMK Firmware

Install pip dependencies
```bash
python3 -m venv env
source env/bin/activate
pip install -r requirements.txt
python -m playwright install chromium

# run it
python main.py
```

